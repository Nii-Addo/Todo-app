import styled from "styled-components";

export const HomeStyle = styled.div`
  width: 100%;
  min-height: 100%;

  .nav {
    z-index: 1;
  }

  .new-todo {
    position: relative;
    left: 28rem;
    margin-top: 0;
    top: 4rem;
    z-index: 1;
  }

  .messages {
  }

  .todos {
    position: relative;
    top: 20rem;
    left: 22rem;
    padding: 5rem 6rem;
  }
`;
